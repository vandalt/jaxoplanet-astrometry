# Relative astrometry with Jaxoplanet

I could not find a relative astrometry or direct imaging tutorial with `jaxoplanet` so I am attempting to write one.

## Install jaxoplanet

```bash
python -m pip install -U -r requirements.txt
```
